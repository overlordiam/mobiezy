// const filters = (wordSearch) => {
//     return data.filter((i) =>
//     i.toLowerCase().includes(wordSearch.toLowerCase())
//   );
// };

// const loadOptions = () => {

//     setTimeout(() => {
//       filters(wordSearch);
//     }, 1000);
//   };

// const handleInputChange = (value) => {
//     const inputValue = value.replace(/\W/g, '');
//     setWordSearch(inputValue);
//   };

{/* <div class="select1">
                    <Select 
                            options={states}
                            style={{"border": "0px"}}
                            placeholder="CITY....."
                            onChange={opt => console.log(opt.label, opt.value)}
                    />
                    </div>
                    <hr style={{"height" : "0.3rem", "backgroundColor": "black"}}></hr>
                    <div class="select1">
                    <Select
                            options={states}
                            style={{"border": "0px"}}
                            placeholder="STATE....."
                            onChange={opt => console.log(opt.label, opt.value)}
                    />
                    </div>
                    <hr style={{"height" : "0.3rem", "backgroundColor": "black"}}></hr>
                    <div class="select1">
                    <Select 
                            options={states}
                            style={{"border": "0px"}}
                            placeholder="OPERATOR....."
                            onChange={opt => console.log(opt.label, opt.value)}
                    />
                    </div>
                    <hr style={{"height" : "0.3rem", "backgroundColor": "black"}}></hr>
                    <CustomButton className="form_btn_2">SUBMIT</CustomButton> */}

                    {/* <Select
          id="country"
          name="country"
          label="country"
          options={updatedCountries}
          value={values.country}
          // onChange={value => {
          //   setFieldValue("country", value);
          //   setFieldValue("state", null);
          //   setFieldValue("city", null);
          // }}
          onChange={(value) => {
            setValues({ country: value, state: null, city: null }, false);
          }}
        /> */}

        //   const countries = Country.getAllCountries();
//   const updatedCountries = countries.map((country) => ({
//     label: country.name,
//     value: country.id,
//     ...country
//   }));

// const img = new Image();
    // img.src = imageUrls;
    // img.onload = () => {
    //     const width = img.width;
    //     const height = img.height;
    //     if ((height > 224) || (width > 224)) {
    //         setDisplayImage(false)
    //         alert("Image size is too large")
    //         setDisplayImage(false)
    //         images.pop()
        // }}